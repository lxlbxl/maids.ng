# Application
APP_NAME="Maids.ng"
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:8000
APP_SECRET=change-this-to-32-char-random-string-in-production

# CORS - Comma-separated list of allowed origins (supports *.domain.com wildcards)
# For production: https://maids.ng,https://admin.maids.ng,https://app.maids.ng
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000

# Session storage: file or redis
SESSION_DRIVER=file
SESSION_LIFETIME=7200

# Redis (for rate limiting, caching, sessions when using Redis)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=null

# Database
DB_DRIVER=sqlite
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=maids_ng
DB_USERNAME=root
DB_PASSWORD=
DB_SQLITE_PATH=../storage/database.sqlite

# Database character set (MySQL only)
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# JWT Configuration (for mobile app support)
JWT_SECRET=your-256-bit-secret-change-this
JWT_ACCESS_TTL=3600
JWT_REFRESH_TTL=2592000

# Flutterwave
FLUTTERWAVE_PUBLIC_KEY=FLWPUBK_TEST-xxxxx
FLUTTERWAVE_SECRET_KEY=FLWSECK_TEST-xxxxx
FLUTTERWAVE_ENCRYPTION_KEY=xxxxx
FLUTTERWAVE_SUBACCOUNT_PERCENTAGE=80  # % of payment that goes to helper

# Paystack
PAYSTACK_PUBLIC_KEY=pk_test_xxxxx
PAYSTACK_SECRET_KEY=sk_test_xxxxx

# N8N Webhooks
N8N_BASE_URL=https://n8n.ai20.city/webhook
N8N_WEBHOOK_SECRET=your-webhook-secret

# QoreID NIN Verification (NIN Premium)
# Get your token from https://dashboard.qoreid.com
QOREID_TOKEN=your-qoreid-token-here

# File Upload
UPLOAD_MAX_SIZE=5242880
UPLOAD_ALLOWED_TYPES=jpg,jpeg,png,gif,webp,pdf

# Storage: local or s3
STORAGE_DRIVER=local
# S3 Configuration (if using S3)
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=
S3_BUCKET=
S3_ENDPOINT=

# Service Fees (in kobo for NGN)
SERVICE_FEE_AMOUNT=10000
SERVICE_FEE_CURRENCY=NGN

# SendGrid Email (get API key from https://sendgrid.com)
SENDGRID_API_KEY=your-sendgrid-api-key
EMAIL_FROM_ADDRESS=noreply@maids.ng
EMAIL_FROM_NAME=Maids.ng

# Termii SMS (get API key from https://termii.com)
TERMII_API_KEY=your-termii-api-key
TERMII_SENDER_ID=Maids.ng
TERMII_CHANNEL=generic

# Admin credentials (change in production)
ADMIN_DEFAULT_EMAIL=admin@maids.ng
ADMIN_DEFAULT_PASSWORD=change-this-on-first-login

# Security
RATE_LIMIT_MAX=60
RATE_LIMIT_WINDOW=60
